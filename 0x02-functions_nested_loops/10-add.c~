#include "main.h"

/**
 *add - adds two integers and returns the result.
 *@a : an input integer.
 *@b : an input integer.
 *Return: Always 0.
 */

int add(int a, int b)
{
	int sum;

	sum =  a + b;

	_putchar(sum + 48);
	return (sum);
}
