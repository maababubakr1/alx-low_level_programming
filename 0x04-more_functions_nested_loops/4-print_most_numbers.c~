#include "main.h"

/**
 *print_most_numbers - prints numbers from 0 yo 9 except 2 & 4
 *
 *Return: Always 0
 */

void print_most_numbers(void)
{
	int i;
	int j;

	for (i = '0'; i <= '9'; i++)
	{
		if (i == '2')
			continue;
		else if (i == '4')
			continue;
		else
			_putchar(i);
	}
	_putchar('\n');
}
